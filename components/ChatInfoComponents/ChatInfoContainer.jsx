import { Container } from "@material-ui/core";
import React from "react";

const ChatInfoContainer = ({ children }) => {
  return <Container>{children}</Container>;
};

export default ChatInfoContainer;
